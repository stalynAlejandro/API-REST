import { StyleSheet } from 'react-native';
import { HEIGHT } from 'constants';

export default StyleSheet.create({
  container: {
    flex: 1
  },
  filterCatalog: {
    flex: 1,
    marginTop: 0
  }
});
